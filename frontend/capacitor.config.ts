import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.financialhq.app',
  appName: 'Financial Headquarters',
  webDir: 'build'
};

export default config;
